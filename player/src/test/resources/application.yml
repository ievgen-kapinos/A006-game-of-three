spring:
  kafka:
    bootstrapServers: localhost:9092
    consumer:
      auto-offset-reset: earliest
      group-id: player
    client-id: player

gof:
  service-name: player
  peer-service-name: peer-player
  max-number: 150
  kafka-events-topic: gof-events   
